{
  "name": "crabd-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "crab'd — a forge-agnostic, multi-provider agent harness for CI",
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:docs": "pnpm --filter @crabd/docs dev",
    "build:docs": "pnpm --filter @crabd/docs build",
    "start:docs": "pnpm --filter @crabd/docs start",
    "build:broker": "pnpm --filter @crabd/broker build",
    "start:broker": "pnpm --filter @crabd/broker start",
    "typecheck": "pnpm -r typecheck",
    "changeset": "changeset"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.1",
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
